<?xml version="1.0"?>
<Container version="2">
  <Name>keystone-web</Name>
  <Repository>ghcr.io/texasdaddy/keystone-web:stable</Repository>
  <Registry>https://github.com/texasdaddy/keystone-web/pkgs/container/keystone-web</Registry>
  <Network>bridge</Network>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support/>
  <Project>https://github.com/texasdaddy/keystone-web</Project>
  <Overview>Keystone Web — the browser frontend for the Keystone platform (Next.js, standalone). A backend-for-frontend: its own server holds the browser session (refresh token in an httpOnly cookie, access token in memory) and calls the keystone API container server-to-server; it never connects to the database. Stateless — no volumes. ONE neutral template for dev and prod: at creation set Name (keystone-web / keystone-web-dev), image tag (:stable / :dev), network mode / static IP, and KEYSTONE_API_URL pointing at the matching keystone API container (http://&lt;api-ip&gt;:8000). Put both containers behind the same public hostname on the reverse proxy: /v1/*, /docs and /openapi.json to the API, everything else to this container — and add this container's IP to the API's TRUSTED_PROXIES so per-IP auth throttling sees the browser rather than this container. Deploy check: GET /api/health reports release/sha plus API reachability.</Overview>
  <Category>Network:Web</Category>
  <!-- No WebUI link on purpose: the session cookie is Secure-only, so opening the container
       directly over plain http signs in and then never restores. Reach it through the reverse
       proxy's public hostname. -->
  <WebUI/>
  <TemplateURL>https://raw.githubusercontent.com/texasdaddy/unraid-templates/main/templates/keystone-web.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/texasdaddy/unraid-templates/main/icons/keystone-web.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <DateInstalled/>
  <Description>Keystone Web — browser frontend (Next.js BFF) for the Keystone platform. Neutral template; set Name (keystone-web / keystone-web-dev), IP, image tag and KEYSTONE_API_URL at creation.</Description>

  <Config Name="HTTP Port" Target="3000" Default="3000" Mode="tcp" Description="App HTTP port (Node standalone server)" Type="Port" Display="always" Required="false" Mask="false">3000</Config>

  <!-- The API this frontend talks to. Server-side only: it is read by the container's own
       Route Handlers and never shipped to the browser, so the internal container address is
       the right value here (no CORS — the browser only ever sees the one public hostname). -->
  <Config Name="KEYSTONE_API_URL" Target="KEYSTONE_API_URL" Default="" Mode="" Description="Base URL of the keystone API container THIS instance talks to, e.g. http://&lt;keystone-api-ip&gt;:8000 (dev web -> dev API). Reached over the container network, never via the public hostname." Type="Variable" Display="always" Required="true" Mask="false"/>

  <!-- Runtime tuning -->
  <Config Name="SESSION_COOKIE_MAX_AGE" Target="SESSION_COOKIE_MAX_AGE" Default="43200" Mode="" Description="Lifetime in seconds of the httpOnly refresh cookie. Keep equal to the API's refresh-token TTL (its JWT_REFRESH_TTL_SECONDS, default 43200 = 12 h): the API's session row is authoritative, so a longer value only leaves a dead cookie behind." Type="Variable" Display="advanced" Required="false" Mask="false">43200</Config>
</Container>
