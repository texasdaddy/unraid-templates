<?xml version="1.0"?>
<!--
  tldw-webui - the browser frontend for tl;dw (Too Long, Didn't Watch), a third-party Next.js
  app: github.com/rmusser01/tldw_server. Not a Keystone service; nothing here is specific to
  any host or operator.

  THREE CONTAINERS, STARTED IN ORDER: tldw-redis, then tldw-server, then tldw-webui. Unraid
  templates have no equivalent of compose's depends_on or healthchecks, so the ordering cannot
  be expressed mechanically - it is stated here and in both siblings' Overviews because that is
  the only place an operator sees it before the fact.

  ⚠️ THE ALIAS REQUIREMENT - READ THIS BEFORE YOU APPLY, OR THE CONTAINER STARTS AND DOES
  NOTHING. The published image has the API origin `http://app:8000` COMPILED INTO ITS BUILD
  OUTPUT (unraid-templates#55) - it is a Next.js standalone build, and the rewrite that sends
  browser API calls to the backend is evaluated once by `next build`, not read from the
  environment at container start. Measured directly against the currently-published `main`
  image (ghcr.io/rmusser01/tldw_server-webui, digest sha256:ae55f346..., resolves to commit
  7a23be3): the literal string is present in the compiled server bundle, and the image's own
  entrypoint (the stock generic Node.js one - nothing custom) does no runtime config
  templating at all. So a template Variable pointed at that setting would be decorative - fill
  it in, Apply, get a container that starts and renders, and every API call silently proxies to
  a name that does not resolve on the stock bridge network. Nothing reports an error; the UI
  just does nothing. This template therefore offers NO field for it.

  The only fix that is both correct and not actively unpleasant (see #55's own options list) is
  a user-defined Docker network with tldw-server ALIASED literally `app` on it. A template
  cannot create that network, and cannot name it either, since the name is host-specific - this
  is a manual, one-time, operator step:
    1. Create a user-defined bridge network on this host (Unraid: Settings > Docker > Network
       Settings, or `docker network create <name-of-your-choosing>`).
    2. At CREATION, set both this container's and tldw-server's Network field to that network
       in the Unraid UI (the Network dropdown only offers networks that already exist on the
       host, which is why this cannot be a template default).
    3. On tldw-server, add the network-alias flag (see this container's Overview / Config
       Description for the exact copy-pasteable text - not repeated here with its own leading
       dashes, which XML comments cannot contain) via "Add another Path, Port, Variable, Label
       or Device" > Extra Parameters. Deliberately NOT shipped as tldw-server's own default:
       that flag only works on a user-defined network, so baking it into an existing,
       already-documented template's shipped Extra Parameters would break that container for
       anyone who applies the update without ALSO doing steps 1-2 first. Opt-in per deployment
       instead of a silent trap on an unrelated template's defaults.
  Skip any of the three and the symptom is exactly the "UI renders, every request goes nowhere"
  failure #55 measured - not a crash, not a log line, just a frontend that never talks to its
  backend.

  A SECOND, SMALLER INERT VARIABLE: `NEXT_PUBLIC_TLDW_DEPLOYMENT_MODE` is also baked into the
  image at build time - any Next.js `NEXT_PUBLIC_*` variable always is, by design, regardless of
  this image's own baked-origin issue. No field for it either, for the same reason.

  NO VOLUME. Nothing in the published image's config or any layer references a persistent
  path - this container is stateless, a pure frontend. There is deliberately no Path Config
  here to go with it.

  NO SEMVER TAG. `ghcr.io/rmusser01/tldw_server-webui` publishes no release-numbered tag at
  all - only `main` and per-commit `sha-*` tags (confirmed live against the GHCR tags API,
  unlike tldw-server's `0.1.38`/`0.1`/`latest`). This template pins `sha-7a23be3`, what `main`
  resolved to at last check - re-verify against the GHCR tags API before assuming it is still
  current if this template has sat unpinned-checked for a while; there is no way to know from
  the tag alone whether it has moved.

  EXTERNAL REACHABILITY IS OUT OF SCOPE FOR THIS TEMPLATE. Publishing the port below makes
  this reachable on the LAN; anything beyond that (a domain, a reverse proxy, TLS) is ordinary
  hostname routing and belongs to whoever administers that, not this file.
-->
<Container version="2">
  <Name>tldw-webui</Name>
  <Repository>ghcr.io/rmusser01/tldw_server-webui:sha-7a23be3</Repository>
  <Registry>https://github.com/rmusser01/tldw_server/pkgs/container/tldw_server-webui</Registry>
  <Network>bridge</Network>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support/>
  <Project>https://github.com/rmusser01/tldw_server</Project>
  <Overview>tl;dw (Too Long, Didn't Watch) - the browser frontend, a Next.js app that talks to the tl;dw API server. THREE CONTAINERS, STARTED IN ORDER: tldw-redis first, then tldw-server, then tldw-webui. There is no template equivalent of compose depends_on, so nothing enforces that ordering for you. ⚠️ REQUIRES A ONE-TIME MANUAL NETWORK SETUP BEFORE THIS WORKS: the published image has its API origin (http://app:8000) compiled into the build output rather than read from the environment (unraid-templates#55), so no field here can wire it - you must create a user-defined Docker network, set BOTH this container's and tldw-server's Network field to it at creation, and add --network-alias=app to tldw-server's Extra Parameters. Skip any of the three and the container starts and renders but every API call silently goes nowhere - see this template's header comment for the exact steps. Two environment variables are inert for the same build-time-baked reason and are deliberately not offered as fields: the API origin above, and NEXT_PUBLIC_TLDW_DEPLOYMENT_MODE. STORAGE: none - this container is stateless, nothing in the published image references a persistent path. IMAGE TAG: upstream publishes no release-numbered tag for this image, only main and per-commit sha-* tags; this template pins sha-7a23be3, verified against the live GHCR tags API - re-check before assuming it is still current if this has sat a while. EXPOSURE: publishing the port below makes this reachable on your LAN; anything further (a domain, reverse proxy, TLS) is ordinary hostname routing, not something this template does - that is a separate, later step for whoever administers your network, not part of getting this container running.</Overview>
  <Category>Productivity: Tools:Utilities</Category>
  <WebUI>http://[IP]:[PORT:3000]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/texasdaddy/unraid-templates/main/templates/tldw-webui.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/texasdaddy/unraid-templates/main/icons/tldw-webui.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <DateInstalled/>
  <Description>tl;dw browser frontend. Needs tldw-redis + tldw-server running first, AND a one-time manual network-alias setup (see the template's own header/Overview) or every API call silently goes nowhere.</Description>

  <Config Name="HTTP Port" Target="3000" Default="8413" Mode="tcp" Description="Host port for the web UI. The container always listens on 3000 internally - change the host side only. The default here is deliberately not 3000, a popular port, so a neutral value avoids a collision on a busy host. ⚠️ THIS CONTAINER DOES NOTHING USEFUL UNTIL A ONE-TIME MANUAL NETWORK SETUP IS DONE: create a user-defined Docker network, set both this container's AND tldw-server's Network field to it (at creation - the Network dropdown only lists networks that already exist), and add --network-alias=app to tldw-server's Extra Parameters. The published image has its API origin compiled in at build time (unraid-templates#55) rather than read from any variable, so skipping this leaves a UI that renders and silently talks to nothing. No volume is offered on this template - it is stateless." Type="Port" Display="always" Required="true" Mask="false">8413</Config>
</Container>
